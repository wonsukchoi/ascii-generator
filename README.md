# ascii-generator
Single-file ASCII art generator. Type text, pick a font, and get large block-style ASCII art instantly. Export as PNG with custom background and text colors. Fully offline — no dependencies, no server, just open index.html.
